apiVersion: v1
kind: Service
metadata:
  name: importador-service
  namespace: gedtotal-apps
  labels:
    app: importador-service
spec:
  type: ClusterIP
  selector:
    app: importador-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
