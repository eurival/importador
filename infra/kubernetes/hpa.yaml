apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: importador-service-hpa
  namespace: gedtotal-apps
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: importador-service-deployment
  minReplicas: 1
  maxReplicas: 1
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 85
